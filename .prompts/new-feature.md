Implement the new feature(s) detailed below.
- Use a temporary git worktree with a name which won't collide with other concurrent agents while building this.
- When finished, prompt for confirmation before merging the changes back to the primary repo

INSTRUCTIONS
Implement the next eval detailed in the spec. Examine current implementation for ways we might have deviated from the spec, and in general follow implementation as it has evolved since spec creation.